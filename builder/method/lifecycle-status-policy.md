# Lifecycle Status Policy

PCR maturity and review lifecycle belong in `manifest.yaml`, not in PCR Markdown sections or `structured.yaml` projections.

- `scaffold`: generated or manually created shell.
- `draft`: authored content exists but has not been reviewed.
- `reviewed`: internally reviewed for modelling use.
- `deprecated`: retained for compatibility but no longer recommended.
