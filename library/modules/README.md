# PCR Method Modules

Reusable modules use the same directory pattern when localized:

```text
library/modules/<group>/<module-slug>/
  manifest.yaml
  module.en.md
  module.zh-CN.md
  structured.yaml
```
